//import ReactSlider from "react-slider";
//import "./Slider.css"
//import { useState } from 'react';
//import CustomHour from "./CustomHour";

/*
function Slider() {

    const [SliderValue, setSliderValue] = useState(80);  
    const [TransValue, setTransValue] = useState(80);


    const changeValue = (value) => {
        setSliderValue(value);
    }

    const changeFinalValue = () => {
        setTransValue(SliderValue);
        console.log("trans value : " + TransValue);
    }

    

    return (
        <div>
            <div>
                <ReactSlider
                    className="horizontal-slider"
                    thumbClassName="example-thumb"
                    trackClassName="example-track"
                    value={SliderValue}
                    step={10}
                    minimumValue={0}
                    maximumValue={100}
                    onChange={changeValue}
                />
                <button onClick={changeFinalValue}>click to store</button>
            </div>
            <div>
                <CustomHour preference={TransValue/100}/>
            </div>
        </div>
    );
};

export default Slider;
*/